/*function showDiv(idDiv){
    let divIds = ['projetsPersonnels', 'projetsProfessionnels'];
    divIds.forEach(divId => {
        let div = document.getElementById(divId);
        if (divId === idDiv){
            div.style.display = (div.style.display === "none") ? "none" : "block";
        }else{
            div.style.display = "none";
        }
    });
}

document.getElementById("btn-personal").addEventListener("click", function () { showDiv("projetsPersonnels"); });
document.getElementById("btn-professional").addEventListener("click", function () { showDiv("projetsProfessionnels"); });
*/